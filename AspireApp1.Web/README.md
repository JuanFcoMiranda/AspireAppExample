# AspireApp1 Web

Vue 3 Composition API app prepared to call the DotNetApi service.

## Dev

```bash
npm install
npm run dev
```

## Configure API base URL

Copy `.env.example` to `.env.local` and adjust the value if needed:

```bash
VITE_API_BASE_URL=http://localhost:51270
```
